# Netlify config. The @astrojs/netlify adapter handles SSR/on-demand routes
# (admin, /api/*) automatically — no manual functions directory needed.
[build]
  command = "npm run build"
  publish = "dist"
